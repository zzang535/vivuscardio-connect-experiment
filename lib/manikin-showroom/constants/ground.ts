// ============================================
// 지면 설정
// ============================================

/**
 * 지면 크기 (가로 x 세로)
 */
export const GROUND_SIZE = {
  /**
   * 지면 너비 (가로)
   */
  WIDTH: 50,

  /**
   * 지면 깊이 (세로)
   */
  HEIGHT: 50,
} as const;

/**
 * 지면 재질 설정
 */
export const GROUND_MATERIAL = {
  /**
   * 지면 색상 (16진수 컬러 코드)
   * - 0x808080: 회색
   * - 0x6a6a6a: 밝은 회색
   * - 0x4a4a4a: 어두운 회색
   * - 0x2a2a2a: 매우 어두운 회색
   */
  COLOR: 0x6a6a6a, // 약간 밝게 조정

  /**
   * 거칠기 (0.0 ~ 1.0)
   */
  ROUGHNESS: 0.8,

  /**
   * 금속성 (0.0 ~ 1.0)
   */
  METALNESS: 0.0,
} as const;
